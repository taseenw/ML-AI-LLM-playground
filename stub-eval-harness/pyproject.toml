[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eval-runner"
version = "0.1.0"
dependencies = [
    "typer>=0.9.0",
    "pyyaml>=6.0",
]

[project.scripts]
eval-run = "eval_runner.cli:app"

[tool.setuptools]
package-dir = {"" = "src"}
